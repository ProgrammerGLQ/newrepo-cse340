<% if (title) { %>
    <h1><%= title %></h1>
  <% } else {
    res.redirect('/')
  } %>
  
  <%- messages() %>
  
  <% if (grid) { %>
    <%- grid %>
  <% } else { %>
    <p class="notice">You have no favorite vehicles.</p>
  <% } %>